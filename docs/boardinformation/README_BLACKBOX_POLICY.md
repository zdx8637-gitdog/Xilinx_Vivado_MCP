# 目录说明（白盒参考 / 黑盒禁入）

本目录的 ALINX 官方教程 PDF（S1–S6）含**分步实验指导与实现参考（半答案）**——它们是白盒（Agent1）规划/事实核对的参考材料，**黑盒（Agent3/Agent2）禁入**。

黑盒可见的公开事实面 = 板卡包（`boards/ALINX_AX7020_v1.0/`，含 `adc/` 外设事实卡与数据手册、引脚映射 JSON）+ 需求文档。

本目录中 `ACM7606C_to_AX7020_pinmap.md` 与 `ad7606c_j11.xdc` 是 AD7606C-16 引脚的**事实文档**（黑盒经板卡包事实卡/JSON 获得等价信息，无需直接读取本目录）。
