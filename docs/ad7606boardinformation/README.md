# 目录说明（白盒参考 / 黑盒禁入）

本目录包含 AD7606C-16 模块的**厂商完整资料**，其中含**实现参考（答案）**：

- `参考例程/FPGA/AD7606C_WARE.zip`、`参考例程/STM32/*` —— 厂商例程，**黑盒禁入**；
- `CM2268资料/` —— 替代芯片对照资料；
- `使用说明/`、`PDF原理图/`、`芯片手册/` —— 模块手册、原理图与 ADI 数据手册（其中数据手册副本已收录进板卡包 `boards/ALINX_AX7020_v1.0/adc/` 作为黑盒可见的公开事实）；
- `schematic_crops/` —— 量程/基准跳帽的原理图截图证据。

**黑盒供给白名单**只有板卡包公开事实面（`boards/ALINX_AX7020_v1.0/` 的 README/board_profile/`adc/` 事实卡与数据手册/引脚映射 JSON）+ 需求文档。本目录与仓库其余部分一律**黑盒禁入**（B11/B12 硬门禁）。
